# Confidence grading

The v1 rubric uses independent corroboration only. Independent supporting evidence
produces `confirmed`; missing/ambiguous evidence produces `suspected`; any
contradiction produces `needs_human_review`. Every assessment must retain the probe,
its evidence, independence rationale, and rubric version so later lab calibration can
adjust thresholds without rewriting history.
